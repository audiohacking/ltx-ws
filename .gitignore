# Python cache
__pycache__/
*.py[cod]
*.pyo

# Virtual environments
.venv/
venv/
env/

# Distribution / packaging
*.egg-info/
dist/
build/

# OS
.DS_Store

# Runtime / local outputs (do not commit)
benchmark_runs/
fvserver_completed/
web_outputs/
web_uploads/
tmp/
# Local MLX weights — gitignored so large checkpoints stay on disk across
# clone/pull; never deleted by the app. Keep your ./models/ folder in the repo.
models/
web/node_modules/
web/dist/
