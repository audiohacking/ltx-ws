# ltx-ws runtime dependencies (Python 3.11+, macOS Apple Silicon for MLX)
websockets>=12.0
av>=12.0
Pillow>=10.0
huggingface_hub>=0.20.0
fastapi>=0.115.0
starlette>=0.37.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
mcp>=1.0.0
mediapipe>=0.10.0

# MLX LTX-2.3 packages are not on PyPI — install from README after the lines above.
